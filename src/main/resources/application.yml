server:
  port: 8080
spring:
  graphql:
    graphiql:
      enabled: true
    path: /graphql

  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:15432/homework1
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver